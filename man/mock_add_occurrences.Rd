% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock_add.R
\name{mock_add_occurrences}
\alias{mock_add_occurrences}
\title{Add clinical-event occurrences to the mock database}
\usage{
mock_add_occurrences(
  con,
  domain,
  person_id,
  concept_id,
  date,
  value = NULL,
  source = c("ehr", "ppi")
)
}
\arguments{
\item{con}{A connection from \code{\link[=mock_aou_connect]{mock_aou_connect()}}.}

\item{domain}{One of "condition", "drug", "measurement", "observation",
"procedure", "device", "visit".}

\item{person_id}{Integer vector of participant ids (one per occurrence).}

\item{concept_id}{Integer concept id(s); recycled to \code{length(person_id)}.}

\item{date}{Event date(s) (Date or coercible); recycled to \code{length(person_id)}.
Used for the domain's start date (and end date, where the table has one).}

\item{value}{Optional numeric value(s) written to \code{value_as_number} where the
table has that column (e.g. measurements, observations).}

\item{source}{\code{"ehr"} tags the \verb{_ext} row as EHR-sourced (\code{src_id} like
"EHR site 105", which EHR-based functions such as
\code{\link[allofus:aou_observation_period]{allofus::aou_observation_period()}} look for); \code{"ppi"} tags it as
survey/physical-measurement ("PPI/PM").}
}
\value{
The generated surrogate ids, invisibly.
}
\description{
Inserts rows into one of the OMOP clinical-event domain tables, generating
surrogate keys automatically and writing the matching \verb{_ext} row (which All of
Us uses to record data source: EHR vs survey/physical-measurement). This is
the primitive for putting custom events into the mock database — e.g. giving a
specific set of participants a specific concept on specific dates.
}
\details{
\code{person_id}, \code{concept_id}, and \code{date} are treated row-wise (recycled to a
common length), so one call inserts one occurrence per \code{person_id} element.
}
\examples{
\dontrun{
con <- mock_aou_connect()
ids <- mock_person_ids(con)[1:50]
mock_add_concepts(con, 201826, "Type 2 diabetes mellitus", domain_id = "Condition")
mock_add_occurrences(con, "condition", person_id = ids, concept_id = 201826,
                     date = as.Date("2021-03-15"))
}
}
