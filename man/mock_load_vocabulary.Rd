% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock_vocabulary.R
\name{mock_load_vocabulary}
\alias{mock_load_vocabulary}
\title{Load a full OMOP vocabulary into the mock database}
\usage{
mock_load_vocabulary(con, dir, tables = NULL, delim = "\\t", quiet = FALSE)
}
\arguments{
\item{con}{A connection from \code{\link[=mock_aou_connect]{mock_aou_connect()}}.}

\item{dir}{Directory containing the Athena vocabulary CSV files.}

\item{tables}{Which vocabulary tables to load. Defaults to all of those found
among \code{concept}, \code{concept_ancestor}, \code{concept_relationship},
\code{concept_synonym}, \code{vocabulary}, \code{domain}, \code{concept_class}, \code{relationship},
and \code{drug_strength}.}

\item{delim}{Field delimiter in the files (Athena uses tab, the default).}

\item{quiet}{Suppress the summary message.}
}
\value{
The names of the tables loaded, invisibly.
}
\description{
By default the mock database ships only a tiny placeholder \code{concept} table and
empty vocabulary tables --- enough to run analyses, but not enough to explore
concept relationships. If your workflow needs the real vocabulary (for
example, finding descendants or ancestors via \code{concept_ancestor}), download
the vocabulary files from the OHDSI \href{https://athena.ohdsi.org}{Athena}
repository and load them with this function. You can then develop and test
ancestor/descendant queries locally instead of on the (metered) Workbench.
}
\details{
Each requested table is \strong{replaced} with the contents of the matching file,
so call this before seeding any custom (non-real) concepts. The files are the
tab-delimited CSVs Athena provides (\code{CONCEPT.csv}, \code{CONCEPT_ANCESTOR.csv},
...); matching is case-insensitive.

Athena vocabulary files are large; loading is done by DuckDB directly from
disk, which is fast, but the resulting database file will grow accordingly.
}
\examples{
\dontrun{
con <- mock_aou_connect()
# after downloading + unzipping an Athena vocabulary bundle:
mock_load_vocabulary(con, "~/Downloads/athena-vocab")

# now descendant/ancestor queries work locally
library(dplyr)
tbl(con, "concept_ancestor") |>
  filter(ancestor_concept_id == 201826L) |>
  inner_join(tbl(con, "concept"),
             by = c("descendant_concept_id" = "concept_id"))
}
}
