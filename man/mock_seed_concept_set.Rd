% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock_seed.R
\name{mock_seed_concept_set}
\alias{mock_seed_concept_set}
\title{Seed a concept set into the mock database}
\usage{
mock_seed_concept_set(
  con,
  concepts,
  domain = "condition",
  prevalence = 0.3,
  max_per_person = 3L,
  values = c(0, 100),
  seed = NULL,
  quiet = FALSE
)
}
\arguments{
\item{con}{A connection from \code{\link[=mock_aou_connect]{mock_aou_connect()}}.}

\item{concepts}{Integer vector of concept ids to seed.}

\item{domain}{One of "condition", "drug", "measurement", "observation",
"procedure", "device", "visit".}

\item{prevalence}{Fraction of participants (0-1) who should have at least one
occurrence. Values below 1 make eligibility joins return partial cohorts.}

\item{max_per_person}{Maximum occurrences generated per selected participant.}

\item{values}{For \code{domain = "measurement"}, a length-2 numeric range for
\code{value_as_number}. Ignored for other domains.}

\item{seed}{Optional random seed for reproducible seeding.}

\item{quiet}{Suppress the summary message.}
}
\value{
The number of occurrence rows inserted, invisibly.
}
\description{
Guarantees that the given concepts exist (in \code{concept} and as occurrences in
the appropriate domain table, with matching EHR \verb{_ext} rows) for a fraction
of participants. Use this so that \code{\link[allofus:aou_concept_set]{allofus::aou_concept_set()}} queries for
your study's concepts return non-empty, develop-able results. Call with the
same \code{concepts}/\code{domain} you pass to \code{aou_concept_set()}.
}
\details{
For finer control (specific participants, specific dates), use the lower-level
\code{\link[=mock_add_occurrences]{mock_add_occurrences()}} / \code{\link[=mock_add_concepts]{mock_add_concepts()}} directly.
}
