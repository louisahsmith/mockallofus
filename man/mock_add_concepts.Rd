% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock_add.R
\name{mock_add_concepts}
\alias{mock_add_concepts}
\title{Add concept definitions to the mock database}
\usage{
mock_add_concepts(
  con,
  concept_id,
  concept_name = NULL,
  domain_id = "Observation"
)
}
\arguments{
\item{con}{A connection from \code{\link[=mock_aou_connect]{mock_aou_connect()}}.}

\item{concept_id}{Integer vector of concept ids.}

\item{concept_name}{Optional character vector of names (parallel to
\code{concept_id}). If \code{NULL}, generic placeholder names are used. Supply real
names when your code searches \code{concept_name} (e.g. by \code{str_detect()}).}

\item{domain_id}{Value for the \code{domain_id} column (e.g. "Condition", "Drug").}
}
\value{
The number of concept rows inserted, invisibly.
}
\description{
Inserts rows into the \code{concept} table for any \code{concept_id}s not already
present. Use this so that concept-name lookups (e.g. searching \code{concept_name})
and joins on \code{concept_id} find your study's concepts. Concepts already present
are skipped, so it is safe to call repeatedly.
}
\examples{
\dontrun{
con <- mock_aou_connect()
mock_add_concepts(con, c(201826, 4193704),
                  c("Type 2 diabetes mellitus", "T2DM without complication"),
                  domain_id = "Condition")
}
}
